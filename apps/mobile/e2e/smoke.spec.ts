// Detox smoke placeholder.
// This test intentionally does not require launching the app yet.

describe('detox smoke', () => {
  it('runs the test runner', async () => {
    expect(1).toBe(1);
  });
});

