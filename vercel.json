{
  "version": 2,
  "buildCommand": "npm run build && cd server && npx prisma migrate deploy && npx prisma generate",
  "installCommand": "npm install && cd server && npm install",
  "outputDirectory": "dist",
  "functions": {
    "api/**": {
      "runtime": "nodejs20.x",
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
